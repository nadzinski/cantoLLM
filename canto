#!/usr/bin/env bash
# Launch the CantoLLM chat CLI from the repo root.
# `uv run` creates/syncs the venv from pyproject.toml as needed.
set -e
cd "$(dirname "$0")"
exec uv run src/cantollm/main.py "$@"
